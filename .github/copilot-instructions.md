# Copilot Coding Instructions for mcp-gitpro

- Use strict TypeScript with ESM imports and `.js` extensions.
- Never add local filesystem editing, local git CLI, shell execution, or browser automation.
- Keep runtime/user-facing text in Turkish.
- Prefer short tool descriptions and structured outputs.
- Use Vitest with mocked GitHub behavior instead of live API calls in CI.
